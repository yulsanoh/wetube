extends base.pug
include mixins/video.pug

block content 
  form(method="GET").search__form
    input(placeholder="Search by title", type="text", name="keyword")
    button
      i.fas.fa-search
  div.video-grid
    each video in videos 
      +video(video)
    else
      span.empty__message 동영상을 찾을 수 없습니다 :(
      