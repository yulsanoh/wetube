header
    a(href="/").header__logo
        i.fab.fa-youtube 
    nav
        ul
            li
                a(href="/search")
                    i.fas.fa-search
            if !loggedIn
                li
                    a(href="/join") 회원가입
                li
                    a(href="/login").header__btn 로그인
            else 
                li
                    a(href="/videos/upload") 동영상 업로드
                li
                    a(href="/users/edit") 프로필 변경
                li
                    a(href="/users/logout") 로그아웃
                li 
                    a(href=`/users/${loggedInUser._id}`)
                        if (loggedInUser.avatarUrl === "")
                            span 😀
                        else 
                            if loggedInUser.socialOnly
                                img.header__avatar(src=`${loggedInUser.avatarUrl}`)
                            else
                                img.header__avatar(src=loggedInUser.avatarUrl, alt="")